import RentalIncomeTaxClient from './page.client';

export const metadata = {
    title: "Rental Income Tax | TaxSimba",
    description: "Learn more about rental income tax with TaxSimba.",
};

const Page = () => {
    return <RentalIncomeTaxClient />;
};

export default Page;
