import SelfEmployedTaxReturnClient from './page.client';

export const metadata = {
    title: "Self-Employed Tax Return | TaxSimba",
    description: "Learn more about self-employed tax returns with TaxSimba.",
};

const Page = () => {
    return <SelfEmployedTaxReturnClient />;
};

export default Page;
